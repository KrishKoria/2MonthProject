---
phase: 01-auth-foundation
plan: "06"
subsystem: auth
tags: [better-auth, drizzle, next.js, rewrites, schema, migration, postgres]

# Dependency graph
requires:
  - phase: 01-auth-foundation
    provides: Better Auth setup, drizzle config, schema.ts foundation, admin plugin wiring
provides:
  - Proxy rewrite that excludes /api/auth/* and /api/admin/* from FastAPI forwarding
  - Combined schema.ts with Better Auth core tables (user, session, account, verification, jwks) plus custom app tables
  - Complete Drizzle migration (0000_whole_maginty.sql) covering all 7 tables including role and banned columns
affects: [02-session-gating, 03-admin-surface, all phases needing auth endpoints]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Negative-lookahead rewrite: /api/((?!auth|admin).*) → FastAPI; /api/auth/* and /api/admin/* stay in Next.js App Router"
    - "Schema-first Better Auth: define BA core tables in schema.ts so drizzle-kit owns the migration"

key-files:
  created:
    - frontend/drizzle/migrations/0000_whole_maginty.sql
  modified:
    - frontend/next.config.ts
    - frontend/src/db/schema.ts
    - frontend/drizzle/migrations/meta/0000_snapshot.json
    - frontend/drizzle/migrations/meta/_journal.json
    - frontend/next.config.test.ts

key-decisions:
  - "Use capturing-group pattern /api/((?!auth|admin).*) with $1 destination — Next.js rewrites require $1 not :path* when source uses a regex capture group"
  - "Manually define Better Auth core tables in schema.ts (CLI prompts interactively; plan provides authoritative definitions for 1.6.x with admin plugin)"
  - "Delete and regenerate migration (not amend) — no production DB at this phase; clean slate is safer and produces a correct single migration"
  - "Pre-existing sse.test.ts failure (bun module-cache issue) is out of scope; logged to deferred-items.md"

patterns-established:
  - "Rewrite exclusion pattern: negative-lookahead /api/((?!prefix1|prefix2).*) keeps Next.js-owned routes in App Router"
  - "BA schema ownership: Better Auth core tables live in schema.ts alongside app tables; drizzle-kit is the single source of truth for migrations"

requirements-completed:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05
  - AUTH-06
  - ACCESS-03
  - ACCESS-04

# Metrics
duration: 3min
completed: 2026-04-15
---

# Phase 01 Plan 06: Gap Closure — Auth Routing and Drizzle Migration Summary

**Negative-lookahead rewrite excludes /api/auth/* and /api/admin/* from FastAPI proxy; schema.ts extended with Better Auth core tables (user/session/account/verification/jwks with role+banned) and full migration regenerated**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-15T18:20:04Z
- **Completed:** 2026-04-15T18:23:38Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Fixed Next.js rewrite rule: `/api/auth/*` and `/api/admin/*` no longer forwarded to FastAPI — Better Auth handler and admin route handlers are now reachable
- Added all Better Auth core tables (user, session, account, verification, jwks) to `schema.ts` with `role` text and `banned` boolean columns on `user`
- Regenerated complete Drizzle migration (`0000_whole_maginty.sql`) covering all 7 tables with correct foreign key constraints
- Updated `next.config.test.ts` to assert the new correct rewrite pattern

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix next.config.ts rewrite** - `0944459` (fix)
2. **Task 2: Add Better Auth core tables and regenerate migration** - `4618c5e` (feat)

**Plan metadata:** TBD (docs: complete plan)

## Files Created/Modified

- `frontend/next.config.ts` - Changed rewrite source to `/api/((?!auth|admin).*)` with `$1` destination
- `frontend/next.config.test.ts` - Updated test assertions to match new rewrite pattern
- `frontend/src/db/schema.ts` - Added Better Auth core tables (user, session, account, verification, jwks) before existing custom tables; added type exports
- `frontend/drizzle/migrations/0000_whole_maginty.sql` - New complete migration with all 7 tables (replaces incomplete `0000_awesome_salo.sql`)
- `frontend/drizzle/migrations/meta/0000_snapshot.json` - Regenerated by drizzle-kit
- `frontend/drizzle/migrations/meta/_journal.json` - Updated with new migration entry

## Decisions Made

- Used `$1` capture-group reference in destination (not `:path*`) — required by Next.js when source uses a regex capturing group
- Manually defined Better Auth tables instead of using `@better-auth/cli generate` because the CLI prompts interactively and cannot run non-interactively in this environment
- Deleted and regenerated migration (clean slate) rather than generating an additive migration — no production DB exists yet in phase 01
- Pre-existing `sse.test.ts` test failure is out of scope (bun module-cache prevents env var override in the second test); logged to `deferred-items.md`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated next.config.test.ts to assert new rewrite pattern**
- **Found during:** Task 2 (running full test suite)
- **Issue:** `next.config.test.ts` still asserted the old `/api/:path*` pattern and `api/:path*` destination after Task 1 changed the rewrite rule; test failed with 1 assertion error
- **Fix:** Updated expected `source` to `/api/((?!auth|admin).*)` and `destination` to `http://127.0.0.1:8000/api/$1`
- **Files modified:** `frontend/next.config.test.ts`
- **Verification:** `bun test` — test now passes (44 pass total, up from 43)
- **Committed in:** `4618c5e` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix — test alignment with corrected implementation)
**Impact on plan:** Required for correctness; no scope creep. The test was the companion assertion for the rewrite rule we changed.

## Issues Encountered

Pre-existing test failure in `frontend/src/lib/sse.test.ts` ("streamInvestigation defaults to NEXT_PUBLIC_API_BASE_URL") was present before this plan. Confirmed by running the test against stashed changes. Cause: bun:test module import caching prevents the second test in the file from seeing the updated `NEXT_PUBLIC_API_BASE_URL` env var. Logged to `deferred-items.md` for a future plan. **This failure is not caused by this plan's changes.**

Final test results: 44 pass, 1 fail (pre-existing), 45 total.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Auth endpoint routing is fixed: `/api/auth/*` reaches Better Auth, `/api/admin/*` reaches Next.js admin handlers
- Database migration is complete: all Better Auth tables plus app tables in one clean migration ready to apply to Neon
- Phase 02 (Session Gating + Entry Pages) can now rely on `GET /api/auth/get-session` returning valid JSON and the `user` table having `role` and `banned` columns
- The pre-existing `sse.test.ts` failure should be addressed before phase 02 work begins (non-blocking for auth but reduces test signal fidelity)

## Self-Check: PASSED

- `frontend/next.config.ts` — FOUND, contains `(?!auth|admin)` pattern
- `frontend/src/db/schema.ts` — FOUND
- `frontend/drizzle/migrations/0000_whole_maginty.sql` — FOUND, contains CREATE TABLE for user (with role+banned), session, account, verification, custom_invitations, access_audit_events
- `01-06-SUMMARY.md` — FOUND
- Commit `0944459` — FOUND
- Commit `4618c5e` — FOUND

---
*Phase: 01-auth-foundation*
*Completed: 2026-04-15*
